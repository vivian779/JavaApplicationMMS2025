 4.6 Compare and contrast the while and for repetition statements.Answer:Featurewhile Loopfor LoopSyntaxwhile (condition) { ... }for (initialization; condition; update) { ... }Use caseSuitable when the number of iterations is unknown beforehandSuitable when the number of iterations is known or fixedStructureLoop continues as long as condition is trueLoop control components are in one line, conciseInitializationMust be done before the loopTypically done in the for statement itselfUpdate statementUsually inside the loop bodyUsually in the for statement after the conditionSummary:•Use for when the number of iterations is known or fixed.•Use while for more flexible, potentially indefinite loops where the exit condition is evaluated before each iteration.
