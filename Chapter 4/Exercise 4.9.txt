4.9 Find and correct the error(s) in each code segment:a)For (i = 100, i >= 1, i++) System.out.println(i);Errors:•The keyword should be lowercase for, not For.•The separator in for loop should be semicolons (;), not commas.•The loop condition i >= 1 with i++ (incrementing) would never decrease i, leading to an infinite loop.Correction:•Use for (i = 100; i >= 1; i--) to count down from 100 to 1.Fixed version:for (i = 100; i >= 1; i--) {
    System.out.println(i);
}b)switch (value % 2) {
case 0:
System.out.println("Even integer");
case 1:
System.out.println("Odd integer");
}Errors:•Missing break; statements, causing fall-through.•No default case (optional but recommended).Correction:switch (value % 2) {
case 0:
    System.out.println("Even integer");
    break;
case 1:
    System.out.println("Odd integer");
    break;
}c)for (i = 19; i >= 1; i += 2)
System.out.println(i);Issue:•Loop adds 2 each time starting from 19. The sequence will increase, not decrease, leading to an infinite loop or incorrect output.Correction:•To get odd numbers from 19 down to 1, decrement by 2:for (i = 19; i >= 1; i -= 2) {
    System.out.println(i);
}d)counter = 2;
do {
System.out.println(counter);
counter += 2;
} While (counter < 100);Errors:•The while keyword should be lowercase, and no space before it.•The condition counter < 100 will only go up to 98, not including 100.Correction:•Use while (counter <= 100); for inclusive output.counter = 2;
do {
    System.out.println(counter);
    counter += 2;
} while (counter <= 100); 